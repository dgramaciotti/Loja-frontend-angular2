import { FiltroProdutosPipe } from './filtro-produtos.pipe';

describe('FiltroProdutosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProdutosPipe();
    expect(pipe).toBeTruthy();
  });
});
